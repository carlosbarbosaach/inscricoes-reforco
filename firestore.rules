rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /horarios/{id} { allow read: if true; allow write: if false; }
    match /inscricoes/{id} { allow read, write: if false; }
  }
}
